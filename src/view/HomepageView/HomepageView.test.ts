import HomepageView from './HomepageView';

describe('HomepageView general test', (): void => {
  it('Class "HomepageView" should be defined', (): void => {
    expect(HomepageView).toBeDefined();
  });
});
